import {mostrarResultado,multiplicar,sumar} from './calculadora.js'; //Dentro de la misma carpeta (.)

let resultado = multiplicar(10,20);
console.log(resultado);


mostrarResultado(multiplicar,10,20,resultado);